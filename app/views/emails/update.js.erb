if (<%= @email.read %>) {
  document.getElementById('<%= @email.id %>').classList.remove("list-group-item-secondary")
} else {
  document.getElementById('<%= @email.id %>').classList.add("list-group-item-secondary")
}